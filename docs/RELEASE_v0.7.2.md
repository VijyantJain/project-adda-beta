
# Adda v0.7.2 — Aura-first field-test preview release
Status: PREVIEW BETA, not automatically iPhone/Android verified. Branch v0.5-full-experience only; main remains isolated. Based on founder screenshots of v0.7.1 Tenacious→0 Aura, empty badges, lost coach tour and silhouette art.
## Root causes fixed
1. Direct Tenacious switched to screen=vibe but never activated any guide; existing guide required crewId. Separate solo-guide state and auto-trigger after actual Aura hydration. Existing v0.7.1 finished solo browsers resume without replaying their ten questions.
2. Expensive getVibe scanned every Crew-related Blob before showing an otherwise solo Starter score and on failure fabricated 0. Fast GET getAuraStarter reads per-participant Starter and guide records and computes the same level curve: 100 answers +30 +50 =180 after ten; all six Starter badges. If a broader Crew score fails, label the partial view and offer honest retry.
3. Aura had no explanation. Aura guide contains meaning, earning actions and earned levels/trophies before Home→Crew explanation→Create explanation→guided Profile. Tour only completes on final acknowledgment. Photo remains optional; emoji/default selectable; no fake account verification.
4. Starter v2 reused silhouette SVGs and bland cards. New users receive v3 question copy and 24 real bundled WebP scenes, with SVG onerror fallbacks, improved two private photo cards. In-progress v1/v2 keep their answer IDs/options/state unchanged.
## Score economics
Starter 10 per distinct answer; +30 at 5; +50 at 10. Two private guide cards +10/+15; each first guided tab +5; first bio/photo-or-avatar/handle draft +10 when legitimately submitted in beta. Crew answer +10, Drop creation +30, chat +4, membership +10, recorded share action +20 (beta does NOT prove invite delivery), active streak +5/day capped 10 days. No points for scrolling. Starter and guide rewards use existing server per-participant ledgers; Crew-derived score retains older beta calculations.
## Acceptance protocol
Direct NEW: clear only test-browser identity; play 1–10 in order; confirm Tenacious 180; press Explore my Aura; confirm ≥180, 6 Starter badges and a non-100% progress to next level; auto Aura guide×3→Home→Crew info→+ info→Profile wizard→Home; photo or emoji/default, bio optional, handle optional. Check background/header/nav taps do not cancel. Refresh at question 6/8 and each tour tab; resume. Return 0.7.1 browser with stored Tenacious but no solo guide; recover without score reset.
Invited NEW: exact /?crew=<id>&drop=<id>; same ten questions; linked Crew/Drop after name; private two cards; two genuine Drop answers when present; guided five screens; original avatars/media/votes remain. Returning Crew mates bypass Starter.
Legacy: in-progress deckVersion=v1/v2 retains answers/options and points. Verify all six Drop formats, unread chat, actual Drop-answer badges, Avengers and old seeded shared five. Check iPhone Safari, Android Chrome, WhatsApp in-app plus narrow desktop, API errors/retry, and photo asset load with no console errors. Dev preview deployment status only proves build, NOT these acceptance tests.
## Data / infrastructure protections
No new Netlify Blob store, no reset or bulk rewrite; no main merge; no fake OTP/profile sync; do not reset persistent test data to reproduce onboarding—use a new browser or storage context. Photo-source IDs in CONTENT_ASSET_REGISTRY.md. CDN-origin photo master rights/release/high-res audit is separate gate before broad commercial launch.
